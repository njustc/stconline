server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/njustc?useUnicode=true&characterEncoding=utf-8
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 数据库自动与jpa同步
    show-sql: true

