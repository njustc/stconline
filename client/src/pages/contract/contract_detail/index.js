import {Button} from 'antd';

/*
export default () => {
	return (
	<div>
		<div>这里能看到合同详情</div>
		<Button>跳转到列表页面</Button>
		<Button>跳转到编辑页面</Button>
	</div>
	);
};
*/

export default () => {
	return <div><div>这里能看到合同详情</div><Button>跳转到列表页面</Button><Button>跳转到编辑页面</Button></div>

};